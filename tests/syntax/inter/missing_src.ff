// missed syntax nodes in syntax and semantic p1 integration tests

const full = ..;
const left = ..1 + 2;
const right = 3 * 5 .. ;

use a::b::c;
var some_tuple = !("hello", 42);

type string {
    len: u64,
    cap: u64,
    data: u64,
}

if 1 != 1 {
}
loop {
}
while true {
}

fn main() {
    type source {
        id: u32,
        src: string,
    }

    fn some_inner(a: i32) -> i32 {
        return 2 * a;
    } 

    return some_inner(read_i32());
}