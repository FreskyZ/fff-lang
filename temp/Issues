Explainer?
Must according to that specification?

`Read` get string not convertable to integer, how to error

Extensions:
    Line comments: line end is \n\r or \n ?
    can delete odd and ++?
    Case to match?
    what's exit?
    Type system design? stop bool to int?
    const to mut?
    struct, method?
    pointer type? with struct, native call?
    stdlib?

Compile to native code, 
    if register is hard to use, just use a stack


Use a DSL to impl a macro, like DSL for and DSL while, it looks like normal code, but is all compile time,
it may make compiler more complex, but used it is more happy
suggest `$` to represent a macro invocation

Suggest data driven rather than runtime reflection